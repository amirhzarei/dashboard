# 🚀 DesignFlow Dashboard

### یک پروژه داشبورد مدیریتی مدرن با React

---

### **✨ معرفی پروژه**

این پروژه، داشبورد مدیریت **DesignFlow**، نمونه‌ای مدرن از یک رابط کاربری مدیریتی است که با تمرکز بر مدیریت داده‌ها و کاربران طراحی شده است. هدف، ایجاد یک بستر امن، سریع و زیبا برای پایش و کنترل داده‌های حیاتی است.

- **نسخه:** `v0.1.0`
- **هدف اصلی:** طراحی و پیاده‌سازی یک **داشبورد مدیریتی (Dashboard)** کامل برای نمایش و مدیریت کاربران.

---

### **📦 نصب و اجرای پروژه**

#### **۱. پیش‌نیازها**

برای اجرای موفقیت‌آمیز پروژه، موارد زیر را در سیستم خود نصب داشته باشید:

- **Node.js:** نسخه‌ی `20` به بالا
- **مدیریت بسته (Package Manager):** `npm` یا `yarn`

#### **۲. نصب وابستگی‌ها**

ابتدا تمام پکیج‌های مورد نیاز پروژه را نصب کنید:

```bash
npm install
# یا
yarn install
```

### **🛠 ساختار و تصمیمات فنی کلیدی**

این پروژه با مجموعه‌ای از تصمیمات معماری و فنی پیاده‌سازی شده است تا حداکثر کارایی، امنیت و توسعه‌پذیری را داشته باشد.

---

#### **۱. مدیریت احراز هویت (Login & Session)**

- **حساب‌های کاربری دمو:** برای تست سریع، دو کاربر ثابت (`admin@test.com` و `owner@test.com`) برای لاگین در نظر گرفته شده است.
- **مدیریت نشست (Session):** یک توکن **جعلی (Mock Token)** امن، در کوکی‌های مرورگر ذخیره شده و وضعیت لاگین کاربر را مدیریت می‌کند.
  - کوکی‌ها به صورت **HttpOnly** و **secure** ذخیره می‌شوند.
- **Middleware محافظتی:** از **Middleware** برای مدیریت دسترسی و فرآیندهای امنیتی استفاده شده است:
  - محافظت از صفحات داشبورد.
  - ریدایرکت کاربران لاگین نشده به مسیر `/login`.
  - مدیریت Logout و پاکسازی توکن‌ها.
  - کنترل دسترسی به Sidebar و صفحات بر اساس **Role** کاربر.

#### **۲. مدیریت خطا (Error Handling)**

یک رویکرد جامع با استفاده از یک **Context مرکزی** برای مدیریت انواع خطاها پیاده‌سازی شده است:

- **Context مرکزی:** **`ErrorProvider`** برای مدیریت کلی خطاها در اپلیکیشن ایجاد شد.
- **دسته‌بندی خطاها:** تمامی خطاها به صورت زیر دسته‌بندی و مدیریت می‌شوند:
  - **Network Error:** اتصال اینترنت ضعیف یا قطع.
  - **Server Error (5xx):** خطاهای سمت سرور از API.
  - **No Data:** وقتی داده‌ای برای نمایش وجود ندارد.
  - **Unauthorized (401):** کاربر لاگین نکرده یا توکن منقضی شده.
- **قابلیت Retry:** امکان **تلاش مجدد (Retry)** دستی برای کامپوننت‌های دریافت‌کننده داده (`fetchable components`) فراهم شده است.
- **اعلان خطا:** استفاده از **Toast** یا **Banner** برای نمایش دوستانه خطاها به کاربر.

#### **۳. کتابخانه‌های اصلی**

| کتابخانه              | هدف استفاده                                                                    |
| :-------------------- | :----------------------------------------------------------------------------- |
| **next**              | فریم‌ورک اصلی React برای قابلیت‌هایی نظیر SSR و SSG.                           |
| **react / react-dom** | ساخت رابط کاربری و مدیریت DOM مجازی.                                           |
| **@supabase/ssr**     | مدیریت آسان‌تر Session و احراز هویت (اختیاری برای مقیاس‌پذیری).                |
| **lucide-react**      | مجموعه آیکون‌های SVG مدرن، سبک و با قابلیت شخصی‌سازی بالا.                     |
| **clsx**              | تسهیل مدیریت کلاس‌های CSS مشروط و داینامیک در JSX.                             |
| **tailwindcss**       | فریم‌ورک Utility-First CSS برای استایلینگ سریع و سازگار.                       |
| **typescript**        | استفاده از تایپ‌سیستم قوی برای افزایش ایمنی کد و جلوگیری از خطاهای زمان توسعه. |

---

### **💡 نکات ساختار و معماری**

#### **۱. مدیریت دیتا و UI**

- **Mock Data:** داده‌ها برای دمو در فایل `mock-data.ts` شبیه‌سازی شده‌اند و به راحتی قابل جایگزینی با API واقعی هستند.
- **کامپوننت‌ها:** کامپوننت‌ها به صورت **reusable** طراحی شده‌اند.
  - کامپوننت‌هایی که نیاز به fetch دیتا دارند، ارورها و loading را مدیریت می‌کنند (استفاده از `useFetch` یا state داخلی).
- **Animated UI:** استفاده از کامپوننت **`AnimatedNumber`** برای جلوه‌های بصری و عددی در داشبورد.
- **کنترل دسترسی:** منوها و Sidebar بر اساس **Role** کاربر رندر می‌شوند.

#### **۲. صفحات و طرح‌بندی (Pages & Layout)**

- **Layout:** فایل `app/layout.tsx` شامل Sidebar و ErrorProvider است.
- **محافظت از صفحات:** Middleware برای محافظت از صفحات داشبورد استفاده می‌شود.
